[Imperative]
{
    def foo(x : int, y : int)
    {
        return = x * y;
    }
    
    def foo(x : int)
    {
        return = x;
    }
    
    def foo(x : int, y : int, z : int)
    {
        return = x * y * z;
    }
    
    y = foo(foo(2, 4, 5), foo(3), foo(1, 3)) + 19;
}